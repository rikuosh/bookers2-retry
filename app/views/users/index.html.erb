<%= render "user_info", user: @user %>
<%= render "books/new", book: @book %>

<h2>Books</h2>
<table class="table">
  <thead>
    <tr>
      <th></th>
      <th>image</th>
      <th>name</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td>image</td>
      <td><%=  user.name %></td>
      <td><%= link_to "Show", user_path(user) %></td>
    </tr>
  </tbody>
</tanle>


<% end %>