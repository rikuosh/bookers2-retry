<h2>Editing Book</h2>
<%= form_with model: @book1, local:true do |f| %>
  <div class="form-group"></div>
  <label for ="book_title">Title</label>
  <%= f.text_field :title %>
  <label for ="book_body">Opinion</label>
  <%= f.text_area :body %>
  <%= f.submit "Update Book" %>
<% end %>


<%= link_to "Show", book_path(@book1) %>
|
<%= link_to "Back", books_path %>